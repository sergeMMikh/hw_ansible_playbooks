---
all:
  children:
    vector:
      hosts:
        first:
          ansible_host: 172.18.0.22
          ansible_user: root
          ansible_ssh_pass: "yourpassword"
    
    clickhouse:
      hosts:
        second:
          ansible_host: 172.18.0.23
          ansible_user: root
          ansible_ssh_pass: "yourpassword"

  

